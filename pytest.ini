[pytest]
# --ignore-glob catches any current + future *_e2e.py (requires Playwright +
# running dev server), so new E2E files don't silently hang the suite.
addopts = -n auto --ignore-glob=btc_web/test_*_e2e.py
testpaths = btc_web
